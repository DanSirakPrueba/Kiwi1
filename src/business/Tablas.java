/* * To change this template, choose Tools | Templates * and open the template in the editor. */package business;import deliver.Deliver;import java.util.ArrayList;/** * * @author Kiwi */public class Tablas {    public static void createTable(Object[] what) {		//Object[] what;		String tableName = (String) what[0];		ArrayList<String> matches = (ArrayList) what[1];		ArrayList<String> names = (ArrayList) what[2];		ArrayList<String> types = (ArryList) what[3];		ArrayList<boolean> optional = (ArrayList) what[4];        String phrase = "{ " + tableName;        for (int i = 0; i < names.size(); i++) {			if (optional.get(i)) phrase += "[";            phrase += " " + matches.get(i) + "." + names.get(i)                     + "." + types.get(i) + " ";            if (optional.get(i)) phrase += "]" + " ";        }        phrase += " }";        Deliver.deliver(Deliver.SYNTAX_AREA, phrase);    }}